@model DataLibrary.Models.OrderModel;
@{
    ViewData["Title"] = "Delete Order";
}

<h1>Delete</h1>

@if (Model == null)
{
    <h4>The requested order was not found.</h4>
}
else
{
    <h4>Are you sure you want to delete your order <i>@Model.OrderName</i> placed on @Model.OrderDate?</h4>

    // The id property of the model gets set by detault to the correct value.
    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>

    @*<form asp-action="Delete">
        <input type="hidden" name="id" value="@Model.Id" />
        <div class="form-group">
            <input type="submit" value="Delete Order" class="btn btn-danger" />
        </div>
    </form>*@
}
